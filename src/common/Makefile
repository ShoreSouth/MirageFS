MODULE := common
LIB    := lib$(MODULE).a

# ============================================================
#  源文件
# ============================================================

SRC := log/fs_log.c
SRC += os/fs_os.c
SRC += path/fs_path.c

include ../../build/build.mk