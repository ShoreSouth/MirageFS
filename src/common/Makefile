MODULE := common
LIB     = $(LIB_DIR)/lib$(MODULE).a

# ============================================================
#  源文件
# ============================================================

SRC := log/fs_log.c
SRC += os/fs_os.c
SRC += path/fs_path.c
SRC += trace/fs_trace.c

include ../../build/build.mk