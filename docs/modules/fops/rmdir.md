# Rmdir 操作

rmdir 负责删除空目录。

## 参数语义

必要输入：

- `parent_fuid`：父目录对象。
- `name`：待删除目录名。
- `flags`：删除控制标志。

## flag 语义

rmdir 推荐支持：

- `FS_FLAG_DIRECTORY`：要求目标是目录。

## 删除条件

rmdir 必须保证：

- 目标存在且是目录。
- 目标目录为空。
- 不允许删除 `.`。
- 对 `..` 的处理必须明确拒绝，不能删除父目录别名。
- 根目录不能被 rmdir 删除。

底层错误应映射为统一 `fs_error_t`。
